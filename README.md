# Public Portfolio (frontend)

Your existing React Hero / skills live here.

## Talk to the API (later)

```text
components/  →  services/  →  Axios  →  backend /api/...
```

Today: `services/` + `data/` mock files.  
Tomorrow: same `getSkills()` function, but `fetch`/`axios` inside the service.

UI components should not care which one it is.
